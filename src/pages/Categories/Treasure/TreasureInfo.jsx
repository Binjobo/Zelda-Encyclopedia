import CommonInfo from "../../CommonInfo";

const TreasureInfo = ({ info, favItems, setFavItems }) => {
  return (
    <CommonInfo info={info} favItems={favItems} setFavItems={setFavItems} />
  );
};

export default TreasureInfo;
